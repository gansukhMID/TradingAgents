services:
  forex-signal-engine:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - forex_engine_data:/home/appuser/.tradingagents

volumes:
  forex_engine_data:
